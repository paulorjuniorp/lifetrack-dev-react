import styled from "styled-components";

const Input = styled.input`
  width: 100%;
  padding: 0.75rem;
  outline: none;
  background: transparent;
  border: none;
  color: #fff;
  border-bottom: 1px solid #6a636a;
`;

export default Input;